 {r}←{quiet}AddGitIgnore path;buff
 r←⍬
 quiet←{0<⎕NC ⍵:⍎⍵ ⋄ 0}'quiet'
 :If quiet
 :OrIf ##.CommTools.YesOrNo'Add .gitignore for APLers to ',path,' ?'
     buff←'aplcore' 'CONTINUE.dws' 'conga.crash'
     (⊂buff)⎕NPUT path,'/.gitignore'
     :If ~quiet
         ⎕←'.gitignore created in ',path,' but check content!'
     :EndIf
 :EndIf

 R←Test_Misc_006(stopFlag batchFlag);⎕TRAP;res
⍝ Runs the "Methods" method
 ⎕TRAP←(999 'C' '. ⍝ Deliberate error')(0 'N')
 R←T._Failed

 res←G.Methods
 →T.PassesIf 2=≡res
 →T.PassesIf 2=⍴⍴res
 →T.PassesIf 3=2⊃⍴res

 R←T._OK
⍝Done

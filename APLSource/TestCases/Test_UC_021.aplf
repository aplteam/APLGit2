 R←Test_UC_021(stopFlag batchFlag);⎕TRAP;stop;rc;msg;hash;C
⍝ Runs ]GetLastCommitHash on the current branch with -full
 ⎕TRAP←(999 'C' '. ⍝ Deliberate error')(0 'N')
 R←T._Failed

 C←∆GetRefToCommTools
 C.AddAutomation'Select' 'SelectPrjForAPLGIT2@' 1
 stop←0
 (rc msg hash)←stop ∆UCMD'GetLastCommit -full'
 msg Assert rc=0
 →T.GoToTidyUp 40≠≢hash

 R←T._OK

∆TidyUp:
 C.Cleanup
⍝Done
